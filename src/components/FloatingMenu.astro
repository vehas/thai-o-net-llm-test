--- 
// No props needed for now 
---

<nav class="floating-menu">
  <a href="#price-correctness" class="menu-item">Price vs Correctness</a>
  <a href="#model-prices" class="menu-item">Model Prices</a>
  <a href="#time-correctness" class="menu-item">Time vs Correctness</a> 
</nav>

<style>
  .floating-menu {
    position: fixed;
    top: 1rem;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(30, 30, 50, 0.85);
    backdrop-filter: blur(5px);
    padding: 0.75rem 1.5rem;
    border-radius: 2rem;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    z-index: 1000;
    display: flex;
    gap: 1.5rem;
    border: 1px solid rgba(var(--accent-light), 15%);
  }

  .menu-item {
    color: #e0e0e0;
    text-decoration: none;
    font-weight: 500;
    padding: 0.3rem 0.6rem;
    border-radius: 1rem;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .menu-item:hover {
    background-color: rgba(var(--accent-light), 20%);
    color: white;
  }

  /* Add styling for the active link (you might need client-side JS for this later) */
  /* Example: .menu-item.active { ... } */
</style>
